{
  "name": "@bookimg/websocket-connection-manager",
  "version": "1.0.0",
  "description": "AWS Lambda for managing WebSocket connections",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../websocket-connection-manager.zip ."
  },
  "dependencies": {
    "@packages/shared": "workspace:*"
  },
  "devDependencies": {
    "typescript": "^5.9.2",
    "@types/node": "^24.3.0",
    "@types/aws-lambda": "^8.10.143",
    "@aws-sdk/client-dynamodb": "^3.879.0",
    "@aws-sdk/client-apigatewaymanagementapi": "^3.879.0"
  }
}
