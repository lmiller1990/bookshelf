{
  "name": "@bookimg/sns-notification-handler",
  "version": "1.0.0",
  "description": "AWS Lambda for handling SNS notifications and WebSocket messaging",
  "type": "module",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "clean": "rm -rf dist",
    "package": "npm run build && cd dist && zip -r ../sns-notification-handler.zip ."
  },
  "dependencies": {
    "@packages/shared": "workspace:*",
    "@aws-sdk/client-dynamodb": "^3.879.0",
    "@aws-sdk/client-apigatewaymanagementapi": "^3.879.0"
  },
  "devDependencies": {
    "esbuild": "^0.23.0",
    "typescript": "^5.9.2",
    "@types/node": "^24.3.0",
    "@types/aws-lambda": "^8.10.143"
  }
}